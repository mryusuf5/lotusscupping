<div class="container d-md-block d-none">
    <div class="my-2 d-flex flex-column justify-content-center align-items-center header">
        <h1 class="header-title">Lotuss Cupping</h1>
        <div class="d-flex gap-2 header-links-container">
            {% for link in links %}
            <a href="#" class="text-decoration-none header-link mx-2 p-2 rounded">{{link}}</a>
            {% endfor %}
        </div>
    </div>
</div>

<div class="d-md-none d-flex justify-content-between align-items-center my-2">
    <div></div>
    <h1 class="m-0">Lotuss Cupping</h1>
    <i class="bi bi-list fs-1"></i>
</div>
